<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.konradlorenz.napa_s">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.ProjectsContainerActivity"/>
        <activity android:name=".Fragments.ProjectTabletDetailFragment"/>
        <activity android:name=".Activities.CreateProjectActivity"></activity>
        <activity android:name=".Activities.TaskDetailActivity"/>
        <activity
            android:name=".Activities.SprintsActivity"/>
        <activity
            android:name=".Activities.ProfileActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activities.RegisterActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>